version: '3.8'
services:
  frontend:
    build: 
      context: ./front
      dockerfile: Dockerfile
      
    networks:
      - my_network
  
  api:
    build: 
      context: ./api
      dockerfile: Dockerfile
    
    env_file:
      - ./api/.env
      - ./uploads:/uploads
    networks:
      - my_network
  
networks:
  my_network:
    external: true
