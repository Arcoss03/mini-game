version: '3.8'
services:
  frontend:
    build: 
      context: ./front
      dockerfile: Dockerfile
    networks:
      - my_network
  
  api:
    build: 
      context: ./api
      dockerfile: Dockerfile
    
    ports:
      - '3000:3000'
    env_file:
      - ./api/.env
    networks:
      - my_network
  
networks:
  my_network:
    external: true
